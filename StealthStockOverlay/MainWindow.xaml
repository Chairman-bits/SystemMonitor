<Window x:Class="StealthStockOverlay.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="System Monitor Helper"
        Width="460"
        Height="320"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual"
        KeyDown="Window_KeyDown">
    <Border CornerRadius="10"
            Padding="12"
            Background="#E8111111"
            BorderBrush="#55FFFFFF"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="HotkeyHelpTextBlock"
                       Text="ホットキーで表示"
                       Foreground="White"
                       FontSize="12"/>

            <TextBlock x:Name="LastUpdatedTextBlock"
                       Grid.Row="1"
                       Foreground="#CCFFFFFF"
                       FontSize="11"
                       Margin="0,6,0,8"
                       Text="更新中..." />

            <ItemsControl x:Name="QuotesItemsControl" Grid.Row="2">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,3,0,3">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="60"/>
                                <ColumnDefinition Width="160"/>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="{Binding DisplaySymbol}"
                                       Foreground="White"
                                       FontFamily="Consolas"
                                       FontSize="15"
                                       VerticalAlignment="Center"/>

                            <TextBlock Grid.Column="1"
                                       Text="{Binding CompanyName}"
                                       Foreground="#E6FFFFFF"
                                       FontSize="13"
                                       TextTrimming="CharacterEllipsis"
                                       VerticalAlignment="Center"
                                       Margin="8,0,8,0"/>

                            <TextBlock Grid.Column="2"
                                       Text="{Binding PriceText}"
                                       Foreground="White"
                                       FontFamily="Consolas"
                                       FontSize="15"
                                       HorizontalAlignment="Right"
                                       VerticalAlignment="Center"/>

                            <TextBlock Grid.Column="3"
                                       Text="{Binding ChangeText}"
                                       Foreground="{Binding ChangeBrush}"
                                       FontFamily="Consolas"
                                       FontSize="15"
                                       HorizontalAlignment="Right"
                                       VerticalAlignment="Center"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock Grid.Row="3"
                       Text="Escですぐ隠す / 右クリックで設定"
                       Foreground="#CCFFFFFF"
                       FontSize="11"
                       Margin="0,8,0,0"/>
        </Grid>
    </Border>
</Window>
